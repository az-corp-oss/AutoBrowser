# Changes 2026-07-14

## Test and Hook Optimizations
- Modified `.husky/task-runner.json` to skip UI tests on pre-push, executing only unit tests (`FullyQualifiedName!~UI`) to speed up git push operations.
- Refactored `src/AutoBrowser.Tests/UI/MainWindowTests.cs` to use robust window fetching in `GetRoutingRuleWindow` (falling back to main window if needed to locate child dialog elements via UI Automation).
- Replaced `.Click()` calls with `.AsButton().Invoke()` in UI tests for reliable button clicks under automated/headless environments.
- Replaced ternary operators and direct calls in tests with explicit `Assert.NotNull` checks to improve test failure visibility.

## Compiler Warnings & Code Quality
- Addressed compiler warning `CS8604` in `AppLauncher.cs`.
- Replaced all empty catch blocks `catch {}` in `AppLauncher.cs` with explicit exception capturing `catch (Exception ex)` and debug logging to avoid "Empty general catch clause suppresses any errors" warnings.
