# Changes 2026-07-13

## UI Test AutomationIds Added
- Added `AutomationProperties.AutomationId` to all controls in `src/AutoBrowser/Views/RuleEditorView.xaml` for FlaUI testability
- Controls: NameBox, PatternBox, BrowserCombo, BrowserPathBox, OkButton, CancelButton
- Fixed `MainWindow_AddRule_FillForm_Save` UI test failure (FlaUI couldn't find controls by x:Name alone)

## Husky.NET Git Hooks Setup
- Installed Husky.NET v0.9.1 as local dotnet tool
- Created `.husky/task-runner.json` with pre-commit task: runs 79 unit tests (excludes UI tests)
- Created `.husky/pre-commit` hook that triggers `dotnet husky run --group pre-commit`
- Hook blocks commit if any unit test fails
- Manual trigger: `dotnet husky run --group pre-commit`
- Teammates setup: `dotnet tool restore` + `dotnet husky install`

## Delete Rule UI Test Added
- Added `MainWindow_DeleteRule` integration test in `src/AutoBrowser.Tests/UI/MainWindowTests.cs` using FlaUI
- The test validates launching the app, adding a rule ("Rule To Delete"), selecting it, clicking the Delete button, and asserting that the rule is no longer present in the UI
