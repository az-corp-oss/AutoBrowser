# Rules

1. Read Serena memories before any task: `AutoBrowser/architecture`, `AutoBrowser/flow`, `AutoBrowser/services`, `AutoBrowser/ui-behavior`
2. Update memories after every change
3. Remove unused usings after code changes (`dotnet format --diagnostics IDE0005`)
4. No comments, no emoji, no commits without permission
5. Use `ShowDialogAsync()` not `System.Windows.MessageBox`
